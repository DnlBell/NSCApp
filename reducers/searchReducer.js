// searchReducer.js
// Holds current current cart, filter and search results

export const searchReducer = (state = {filter: {}, results: []}, {filter, results}) => {
    if (type === 'SEARCH_RESULTS') {
        // state = unionWith(state, {username, token};
        }

        if (type === 'SEARCH_UPDATE_FILTER') {
        // state = unionWith(state, {username: null, token: null});
        }

        if (type === 'SEARCH_CLEAR_FILTER') {

        }

        if (type === 'SEARCH_CLEAR_RESULTS') {
            //state = unionWith(state, {results: []});
        }

        return state;
};

